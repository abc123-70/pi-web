# pi-web 防自杀机制（优雅重启）

> **问题背景**：网页版 AI 会话运行在 `node server.mjs` 进程内部。AI 若用 `taskkill` 强杀该进程（或直接重启），会把自己的回复流掐断，表现为"回复截断、页面回不去"。

## 一、禁止行为

- ❌ **禁止用 `taskkill //F //PID <server>` 杀掉 8765 端口的 node 进程**（那是 AI 自己的宿主进程）
- ❌ 禁止在回复过程中直接调用重启（会截断当前回复）

## 二、正确重启方式（AI 或用户均可用）

```bash
# 方法 1：延迟 8 秒触发，先让当前回复完整发完，再优雅重启
cmd //c "timeout /t 8 /nobreak >nul & curl -s -X POST http://127.0.0.1:8765/api/restart"

# 方法 2：立即优雅重启（不推荐在回复中直接用）
curl -s -X POST http://127.0.0.1:8765/api/restart
```

**`/api/restart` 做了什么**（见 `server.mjs` 对应路由）：

1. 向所有已打开页面广播 SSE `reload` 事件（页面自动刷新，无需手动）
2. 返回 `200 {"ok":true}` 确认
3. 300ms 后：释放所有 agent 会话（会话状态已持久化到 jsonl 文件，不丢上下文）
4. spawn 新进程接管同端口 → 旧进程退出

## 三、重启后

- 新进程自动恢复最近会话（从 `~/.pi/agent/sessions/*.jsonl` 加载）
- 已打开页面收到 `reload` 自动刷新，SSE 自动重连
- **对话上下文不丢失**（jsonl 持久化 + 懒恢复机制）

## 四、何时需要重启

| 修改内容 | 是否需要重启 |
|---------|------------|
| `page.html`（CSS/HTML/JS） | ❌ 不需要，热更新自动生效 |
| `server.mjs`（后端逻辑） | ✅ 需要，用上面的方法 1 |

## 五、验证方法（独立脚本）

不要在自己的会话进程里直接调 restart 验证（会截断自己的回复）。用独立脚本：

```bash
node restart-test.mjs   # 独立进程：调用 /api/restart → 等待 → 检查新 PID 与首页状态
```

结果写入 `restart-test-result.txt`。

---

# 无想法气泡规范（隐私干净界面）

> **需求**：用户要求界面完全不展示 AI 的思考过程——思考时不留任何气泡，正文出现才显示。

## 机制（page.html）

1. 服务端 `stripThinking()` 已剥离消息里的 `thinking` 块，前端收不到思考文本
2. 前端 `message_start` **不渲染空气泡**（只记录状态）——避免 AI 思考期间出现空白气泡框
3. `updateLastAssistant`：正文为空（思考中/仅工具调用）→ **直接 return，不渲染**
4. `renderMsg`：空正文消息 → 不创建气泡元素（兑底）
5. 气泡行用 `data-msg-id` 精确定位更新，避免误覆盖历史消息

## 验收方法（Playwright）

模拟事件流验证：`message_start` → 思考（空 update ×N）→ 正文到达：

- message_start 后气泡数不增加 ✓
- 思考中多次 update 气泡数不变 ✓
- 正文到达恰好 +1 气泡，内容正确 ✓
- 历史消息不被覆盖 ✓

---

# 回复样式统一规范（朴素对话）

> **需求**：AI 对话回复不出现加粗文字、表情符号、深底彩字（如行内代码蓝字）、引用蓝边等花哨格式，全部统一为普通正文。

## 机制（page.html）

- `strong` / `b`：`color: inherit; font-weight: 400`（不加粗、同正文色）
- `em` / `i`：`font-style: normal; color: inherit`（不斜体）
- 行内代码 `code.md-inline`：`background: transparent; border: none; font: inherit; color: inherit`（与正文完全一致）
- 标题 `h1-h6`：`color: inherit; font-weight: 400`（仅字号略区分）
- 引用 `blockquote`：无背景、无蓝边（边框用普通 `#2a323d`）
- 表格表头 `th`：`font-weight: 400`，颜色继承

## 约定（对话回复）

- 禁止表情符号（✅⚠️📋 等）与装饰性符号
- 禁止 markdown 加粗/斜体/标题；代码块只用于真正的代码
- 行内代码尽量少用（仅文件名/命令等必须场景）
- 回复用纯文本平铺直叙，直接给结论

## 验收方法（Playwright）

渲染一段含 `**加粗**`、`code`、`## 标题`、表格、引用的文本，检查 computed style：所有元素颜色 = body 色 `rgb(232,234,237)`，font-weight = `400`，行内代码/引用背景透明，引用无蓝边。
